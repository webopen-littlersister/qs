# qs
#仿qs模块
```
使用1 -S
npm  i webopen-littlesister-qs -S
const  qs = require('webopen-littlesister-qs -S')
语法1 : qs.parse(参数去问号)
作用1 :将get参数去问号转对象

语法1 : qs.stringify(对象)
作用1 :将对象转get参数去问号
```
```
使用2 -g
npm  i webopen-littlesister-qs -g

语法 : qs 'get参数去问号'
作用 :将get参数转对象

```
